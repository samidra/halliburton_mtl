import { FilterNotSelectedPipe } from './filter-not-selected.pipe';

describe('FilterNotSelectedPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterNotSelectedPipe();
    expect(pipe).toBeTruthy();
  });
});
